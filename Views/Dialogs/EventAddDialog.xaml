<Window x:Class="ToyShop.Views.UserControls.EventAddDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавление акции" Height="500" Width="550"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#e1e1e1">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Название акции:" FontSize="14" FontWeight="Bold" 
                   Foreground="#000000" Margin="0,0,0,5"/>
        <TextBox x:Name="tbEventName" Grid.Row="1" Height="35" Margin="0,0,0,15"/>

        <Grid Grid.Row="2" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Дата начала:" FontSize="14" FontWeight="Bold" 
                       VerticalAlignment="Center" Margin="0,0,10,0"/>
            <DatePicker x:Name="dpStartDate" Grid.Column="1" Height="35" Margin="0,0,15,0"/>

            <TextBlock Grid.Column="2" Text="Дата окончания:" FontSize="14" FontWeight="Bold" 
                       VerticalAlignment="Center" Margin="0,0,10,0"/>
            <DatePicker x:Name="dpEndDate" Grid.Column="3" Height="35"/>
        </Grid>

        <TextBlock Grid.Row="3" Text="Выберите категории и скидки:" FontSize="14" FontWeight="Bold" 
                   Foreground="#000000" Margin="0,0,0,10"/>

        <ScrollViewer Grid.Row="4" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="spCategories"/>
        </ScrollViewer>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="OK" Width="80" Height="35" Margin="0,0,10,0"
                    Click="BtnOk_Click" Background="#b5d5ca" Foreground="#000000"/>
            <Button Content="Отмена" Width="80" Height="35"
                    Click="BtnCancel_Click" Background="#e0a9af" Foreground="#000000"/>
        </StackPanel>
    </Grid>
</Window>