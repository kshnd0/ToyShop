<Window x:Class="ToyShop.Views.UserControls.ProviderDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Поставщик" Height="350" Width="467"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#e1e1e1"
        Icon="/Images/logo.png">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Название:" 
                   FontSize="14" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,10"
                   Foreground="#000000"/>
        <TextBox x:Name="tbName" Grid.Row="0" Grid.Column="1" Height="35" Margin="0,0,0,10"
                 Background="White" BorderBrush="#b5d5ca" BorderThickness="1"/>

        
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Телефон:" 
                   FontSize="14" FontWeight="Bold" VerticalAlignment="Top" Margin="0,8,10,0"
                   Foreground="#000000"/>
        <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,0,10">
            <TextBox x:Name="tbNumber" Height="35" 
                     Background="White" BorderBrush="#b5d5ca" BorderThickness="1"/>
            <TextBlock Text="Формат: 7-XXX-XXX-XX-XX (пример: 7-495-123-45-67)" 
                       FontSize="10" Foreground="Gray" Margin="5,3,0,0"/>
        </StackPanel>

        
        <TextBlock Grid.Row="2" Grid.Column="0" Text="ИНН:" 
                   FontSize="14" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,10"
                   Foreground="#000000"/>
        <TextBox x:Name="tbInn" Grid.Row="2" Grid.Column="1" Height="35" Margin="0,0,0,20"
                 Background="White" BorderBrush="#b5d5ca" BorderThickness="1"/>

        
        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="80" Height="35" Margin="0,0,10,0"
                    Click="BtnOk_Click" Background="#b5d5ca" Foreground="#000000"/>
            <Button Content="Отмена" Width="80" Height="35"
                    Click="BtnCancel_Click" Background="#e0a9af" Foreground="#000000"/>
        </StackPanel>
    </Grid>
</Window>